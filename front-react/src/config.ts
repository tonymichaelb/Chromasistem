/**
 * Modo MOCK: quando true, todas as chamadas fetch para /api/* são respondidas
 * com dados mockados, permitindo testar o front (UI/UX) sem o backend.
 * Coloque false para usar a API real.
 */
export const MOCK = false
